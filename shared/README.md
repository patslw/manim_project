# Shared Components

This directory contains tools and components intended for reuse across video
projects.

Keep shared changes separate from video-specific changes. Add `#base` to the
commit message only after the shared code works without files from `project/`.

- `manim/` contains reusable Manim components and helpers.
- `vscode/` contains reusable editor integration.
